#include <stdio.h> //header 선행처리기 : 도입부

#define ADD(x,y) ((x)+(y)) 
/*ADD 라는 매크로를 정의하고 인수가 x, y 
   수식이 ((x)+(y)) 진행된다. */
#define SUB(x,y) ((x)-(y))
/*SUB 라는 매크로를 정의하고 인수가 x, y 
   수식이 ((x)-(y)) 진행된다. */